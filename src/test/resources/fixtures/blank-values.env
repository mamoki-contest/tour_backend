# .env.example 을 그대로 복사한 .env 를 재현한다.
# 값이 빈 줄이 실제 파일에서와 같은 방식(properties 형식)으로 읽힌다.
DATA_LAB_LAG_DAYS=
KOR_SERVICE_BASE_URL=
ITS_BASE_URL=
ITS_HALF_SPAN=
NAVER_AMBIGUOUS_RATIO=

# 공백만 있는 줄도 비어 있는 것으로 본다.
DATA_LAB_BASE_URL=

# 채운 값은 그대로 쓰여야 한다. 기본값은 7 이다.
DATA_LAB_WINDOW_DAYS=5
